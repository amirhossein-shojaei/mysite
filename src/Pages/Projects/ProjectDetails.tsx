import { Link, useParams } from 'react-router-dom'; import { projects } from '../../data/projects'; import { ArrowLink, SectionTitle } from '../../components/Shell'; import { useLanguage } from '../../locales'
export function ProjectDetails(){const {id}=useParams(); const {t,language}=useLanguage(); const p=projects.find(x=>x.id===id); const arrow=language==='fa'?'←':'→'; if(!p)return <section className="page container"><p className="eyebrow">404</p><h1>{t('Project not found.')}</h1><Link className="text-link" to="/projects">{t('Back to projects →')}</Link></section>; const blocks=[['Project Overview',p.description],['Client / Organization',p.client],['Problem',p.problem],['Solution',p.solution],['My Contribution',p.contribution]]; return <section className="page container detail"><Link className="back" to="/projects">{t('Back to projects')} {arrow}</Link><div className="detail-head"><div><p className="eyebrow">{t(p.category)}</p><h1>{p.title}</h1><p className="lead">{t(p.description)}</p></div><div className="detail-links">{p.projectUrl&&<ArrowLink href={p.projectUrl}>{t(p.id==='elixia'?'Visit Project':'Open Application')}</ArrowLink>}{p.organizationUrl&&<ArrowLink href={p.organizationUrl}>{t('Official Website')}</ArrowLink>}</div></div><div className="detail-art"><span>{p.title.slice(0,2).toUpperCase()}</span><small>{t(p.type)}</small></div><div className="detail-grid">{blocks.map(([h,v])=><div className="detail-block" key={h}><p className="eyebrow">{t(h)}</p><p>{t(v)}</p></div>)}<div className="detail-block"><p className="eyebrow">{t('Features')}</p><ul>{p.features.length?p.features.map(f=><li key={f}>{t(f)}</li>):<li>{t('Details will be added later.')}</li>}</ul></div><div className="detail-block"><p className="eyebrow">{t('Technology Stack')}</p><div className="tags">{p.technologies.length?p.technologies.map(x=><span key={x}>{t(x)}</span>):<span>{t('Details will be added later.')}</span>}</div></div></div></section>}
